# Overlay Packages

Overlay packages for fparted-toolchain belong in this directory.

Each subdirectory contains an overlay package that supplements or modifies the default Termux packages used in the rootfs build. Overlays are copied into the Termux checkout directory during the build process.
